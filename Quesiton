Bonjour à tous,

Checkmarx a remonté plusieurs vulnérabilités CSRF sur des endpoints modifiant l’état (POST/PUT/DELETE).

Étant donné que Timesheet utilise une authentification JWT Bearer (sans cookies de session), pensez-vous que cette vulnérabilité soit réellement exploitable dans notre contexte ?

Merci d’avance pour vos retours.
